# Database connection (used by Prisma)
DATABASE_URL=

# NextAuth secret (random 32-byte hex)
NEXTAUTH_SECRET=

# URL of your deployed app (e.g., for OAuth callback)
NEXTAUTH_URL=

# (Optional) Public base URL for client-side code
NEXT_PUBLIC_BASE_URL=

# (Optional) Email SMTP settings for nodemailer
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=